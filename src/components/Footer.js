import React from 'react'

import styles from "./Footer.module.css";

const Footer = () => {
  return <footer className={styles.footer}>
    <h3>
        Escreva sobre oque você tem interesse        
    </h3>
    <span>Miniblog</span>
  </footer>
}

export default Footer